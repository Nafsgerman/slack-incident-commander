{
  "name": "slack-incident-commander",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Slack-native incident response agent for the Slack Agent Builder Challenge.",
  "scripts": {
    "demo": "node src/demo/run-demo.ts",
    "test": "node --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
